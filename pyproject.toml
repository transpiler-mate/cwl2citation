# Copyright 2026 Transpiler-Mate
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "cwl2citation"
dynamic = ["version"]
description = "Generate software citations from CWL metadata."
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [
  { name = "Transpiler-Mate", email = "info@terradue.com" },
]
keywords = []
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = [
  "transpiler-mate-api==1.0.0",
  "loguru==0.7.3",
  "citeproc-py>=0.11.1,<0.12",
  "citeproc-py-styles==0.1.6",
  "bibtexparser>=2.0.1,<3",
  "rispy>=0.10,<0.11",
  "pylatexenc>=2.10,<3",
  "jsonschema>=4.23,<5",
  "ruamel.yaml>=0.18,<0.20",
  "pydantic>=2,<3"
]

[project.optional-dependencies]
test = [
  "pytest>=8.4,<10",
  "pytest-cov>=6.2,<8",
  "cwl-loader>=0.25.0",
  "transpiler-mate-runtime>=1.0.1,<2",
]

[project.entry-points."transpiler_mate.plugins"]
cwl2citation = "cwl2citation.plugin:cwl2citation"

[project.urls]
Homepage = "https://github.com/Transpiler-Mate/cwl2citation"
Documentation = "https://Transpiler-Mate.github.io/cwl2citation/"
Repository = "https://github.com/Transpiler-Mate/cwl2citation"
Issues = "https://github.com/Transpiler-Mate/cwl2citation/issues"
Changelog = "https://github.com/Transpiler-Mate/cwl2citation/blob/main/CHANGELOG.md"

[tool.hatch.version]
path = "src/cwl2citation/__about__.py"

[tool.hatch.build.targets.sdist]
include = [
  "/src",
  "/tests",
  "/docs",
  "/README.md",
  "/CHANGELOG.md",
  "/LICENSE",
  "/NOTICE",
  "/THIRD_PARTY.md",
  "/pyproject.toml",
  "/mkdocs.yaml",
  "/requirements-docs.txt",
  "/CONTRIBUTING.md",
]

[tool.hatch.build.targets.wheel]
packages = ["src/cwl2citation"]

[tool.hatch.envs.types]
features = ["test"]
extra-dependencies = [
  "mypy==1.19.1",
]
[tool.hatch.envs.types.scripts]
check = "mypy --install-types --non-interactive {args:src/cwl2citation tests}"

[tool.coverage.run]
source_pkgs = ["cwl2citation"]
branch = true
parallel = true
omit = [
  "src/cwl2citation/__about__.py",
]

[tool.coverage.paths]
cwl2citation = ["src/cwl2citation", "*/cwl2citation/src/cwl2citation"]

[tool.coverage.report]
exclude_lines = [
  "no cov",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
]

[tool.hatch.envs.test]
skip-install = false
features = ["test"]

[tool.hatch.envs.test.scripts]
test = "pytest {args:tests}"
testv = "pytest --verbose {args:tests}"
test-cov = "pytest --cov=cwl2citation --cov-report=term-missing {args:tests}"
test-show-setup = "pytest --setup-show"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
  "--strict-config",
  "--strict-markers",
]

[[tool.hatch.envs.test.matrix]]
python = ["3.10", "3.11", "3.12", "3.13", "3.14"]

[tool.hatch.envs.dev]
features = ["test"]
skip-install = false
dependencies = [
  "mypy>=2.3.0",
  "ruff>=0.16.0",
  "bandit>=1.9.4",
]

[tool.ruff.lint]
# Start with the essential rules
select = [
  "F",    # Pyflakes (Critical for bugs)
  "E",    # pycodestyle Errors
  "W",    # pycodestyle Warnings
  "I",    # isort (Import sorting)
  "UP",   # pyupgrade (Modern syntax)
  "B",    # flake8-bugbear (Common bugs)
  "C4",   # flake8-comprehensions
  "SIM",  # flake8-simplify
  "FA",   # flake8-future-annotations
  "TC",   # flake8-type-checking
  "PTH",  # flake8-use-pathlib
  "RET",  # flake8-return
  "C90",  # McCabe Complexity (Enables C901)
]

# Ignore rules that conflict with the formatter (Ruff Format/Black)
ignore = [
  "E501", # Line too long (handled by formatter)
  "W191", # Tab indentation (handled by formatter)
  "E111", # Indentation issues (handled by formatter)
  "E114", # Indentation issues (handled by formatter)
  "E117", # Over-indented (handled by formatter)
]

[tool.ruff.lint.mccabe]
# Flag errors (C901) whenever complexity exceeds 10 (default is 10)
max-complexity = 10

[tool.hatch.envs.dev.scripts]
typecheck = "mypy --install-types --non-interactive {args:src tests}"
lint = "ruff format {args:src tests}"
check = "ruff check --fix {args:src tests}"
security = "bandit -r {args:src}"

[tool.mypy]
strict = true

[[tool.mypy.overrides]]
module = ["cwl_loader", "cwl_loader.*", "transpiler_mate.runtime.*", "citeproc.*", "citeproc_styles.*", "rispy", "pylatexenc.*", "jsonschema"]
ignore_missing_imports = true
