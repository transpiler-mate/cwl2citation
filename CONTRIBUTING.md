# Contributing

Use Python 3.10+ and install `pip install -e '.[test]'`. Follow the checks listed in README.md. Add parser-based exporter tests when changing serialization and CLI tests when changing options. Preserve shared metadata semantics across all formats.

Bundled upstream schemas must retain their original licenses and attribution. Update THIRD_PARTY.md and schema checksums when updating them.
